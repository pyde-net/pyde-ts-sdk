# Build output (git-ignored, regenerated by tsup)
dist/
coverage/

# Dependencies
node_modules/

# Generated npm lockfile — npm owns its formatting
package-lock.json

# Vendored pyde-crypto-wasm — must stay byte-identical to the upstream
# wasm-bindgen output; never reformat it.
src/vendor/

# Binary
*.wasm
